---
- include: deploy.yml hosts="release.antihero-game.com" base=/home/ec2-user/realtime/release
- include: restart.yml hosts="release.antihero-game.com" base=/home/ec2-user/realtime/release hash="{{ hostvars['localhost']['git_rev'].stdout }}"
